import React from 'react';
import ReactDOM from 'react-dom';

// add and remove a debugger statement here
// - repeat with/without it
// - try adding a VSCode breakpoint on the console.log statement. see if it takes when debugging.
console.log('bananas')
